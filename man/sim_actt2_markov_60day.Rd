% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-markov.R
\name{sim_actt2_markov_60day}
\alias{sim_actt2_markov_60day}
\title{Simulate 60-day ACTT-2 calibrated Markov trajectories}
\usage{
sim_actt2_markov_60day(
  n_patients = 1000,
  treatment_prob = 0.5,
  treatment_effect = -0.00344081587038234,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_patients}{Number of patients to simulate. Must be a positive integer.}

\item{treatment_prob}{Probability of assignment to the active arm (\code{tx = 1}).
Must be a numeric scalar between 0 and 1.}

\item{treatment_effect}{Shared proportional-odds coefficient applied to the
active arm. The default is the fitted null-calibration estimate
-0.0039178706.}

\item{seed}{Optional random-number seed. The seed is set once before baseline
states and treatment assignments are sampled, and trajectory generation
continues from the same random-number stream.}

\item{...}{Advanced overrides passed to \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. These can
override defaults such as \code{follow_up_time}. Supplying \code{baseline_data}
replaces the generated baseline rows.}
}
\value{
A data frame with the same columns returned by
\code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. The default output contains 60 rows per
patient and states numbered 1 through 8.
}
\description{
Generates 60-day eight-state trajectories from a full proportional-odds
Markov model calibrated to 100,000 patients generated by
\code{\link[=sim_trajectories_brownian_gap]{sim_trajectories_brownian_gap()}}. The calibration model was
\code{y ~ tx + yprev + rms::rcs(time, 7)} and used 5,565,851 non-post-absorption
transitions.
}
\details{
Baseline states 4 through 7 are sampled with probabilities 0.04797, 0.46254,
0.35816, and 0.13133, estimated from the 100,000-patient Brownian-gap
calibration cohort. State 8 is absorbing.

The fitted restricted cubic spline uses knots at days 2, 11, 20, 30, 39, 49,
and 59. Printed \code{rms::orm()} thresholds are reversed only at the boundary to
\code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}; all fitted slopes retain their reported signs.
Treatment is coded as \code{tx = 0} for placebo/control and \code{tx = 1} for active
treatment. Because higher states are worse, a negative treatment coefficient
is protective.

This full proportional-odds transition model is a compact approximation to
the Brownian-gap source process, not an occupancy-equivalent replacement. In
the calibration check, source and wrapper day-60 mortality were 9.58 and
15.55 percent, respectively, and the maximum absolute state-by-day difference
was 25.18 percentage points. Use \code{\link[=sim_actt2_brownian]{sim_actt2_brownian()}} when the source
Brownian dynamics are required. Follow-up beyond day 60 is extrapolation
outside the fitted time support.
}
\examples{
trajectories <- sim_actt2_markov_60day(n_patients = 20, seed = 123)
head(trajectories)

}
