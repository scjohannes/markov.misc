% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-standardize.R
\name{standardize_sops}
\alias{standardize_sops}
\title{Compute standardized state occupancy probabilities (Vectorized)}
\usage{
standardize_sops(
  model,
  data = NULL,
  times = NULL,
  ylevels = factor(1:6),
  absorb = 6,
  varnames = list(tvarname = "time", pvarname = "yprev", p2varname = NULL, id = "id", tx
    = "tx", gap = NULL),
  t_covs = NULL,
  include_re = FALSE,
  id_var = NULL,
  n_draws = 100L,
  seed = NULL
)
}
\arguments{
\item{model}{A fitted model object (orm, vglm, rmsb). For \code{vglm} models,
the family must be \code{cumulative(reverse = TRUE, ...)}.}

\item{data}{A data frame containing patient trajectory data.}

\item{times}{Visit-scale time points to predict. Factor-valued visit
indices use fitted factor levels when \code{times = NULL}.}

\item{ylevels}{States in the data.}

\item{absorb}{Absorbing state name.}

\item{varnames}{List of variable names: tvarname, pvarname, p2varname
(optional second previous-state variable), id, tx, gap (optional).}

\item{t_covs}{Time-dependent covariate lookup table.}

\item{include_re}{Logical. For \code{rmsb::blrm()} fits with \code{cluster()}, include
fitted random-effect draws in posterior predictions.}

\item{id_var}{Optional patient ID variable. Defaults to \code{varnames$id}.}

\item{n_draws}{Integer number of \code{blrm} posterior draws to sample, or \code{NULL}
to use all stored draws.}

\item{seed}{Optional random seed for reproducible \code{blrm} draw sampling.}
}
\value{
A list with two components:
\item{sop_tx}{Matrix (Time x State) or Array (Draws x Time x State) for Treatment}
\item{sop_ctrl}{Matrix (Time x State) or Array (Draws x Time x State) for Control}
}
\description{
Updates the standardize_sops wrapper to use the optimized
soprob_markov_vectorized function.
}
