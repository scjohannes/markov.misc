% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_trajectories.R
\name{recurr_event}
\alias{recurr_event}
\title{Generate recurrent event times for individuals (helper for latent TTE DGM)}
\usage{
recurr_event(
  id,
  dist = "Exponential",
  param,
  b = 0,
  follow_up = 60,
  max_events = NULL
)
}
\arguments{
\item{id}{Optional numeric vector of subject identifiers. If supplied, these
identifiers are repeated \code{max_events} times internally (one block of
rates per subject). If missing, identifiers 1:n are generated where
\code{n} is provided or inferred.}

\item{dist}{Character. Name of the waiting-time distribution. Currently only
"Exponential" is supported (default).}

\item{param}{Numeric. Parameter vector for the waiting-time distribution.
The first element is taken as the baseline rate lambda used to
construct the per-event rates. Only \code{param[1]} is used by the current
implementation.}

\item{b}{Numeric scalar. Increment added to the rate for every subsequent
event (autoregressive Poisson / accelerating rates). Rate for event j is
lambda + b * (j - 1). Default \code{0} (constant rates).}

\item{follow_up}{Numeric scalar. Administrative follow-up time. Event times
\code{>= follow_up} are censored and not returned. Default \code{60}.}

\item{max_events}{Optional integer. Maximum number of events to generate per
individual. If NULL (default) the function chooses a suitable \code{max_events}
between 3 and 20 so that the probability of observing all events within
\code{follow_up} is very small (threshold 1e-4). When provided, the function
uses the given value.}
}
\value{
A two-column numeric matrix / data.frame with columns
\itemize{
\item \code{id}: subject identifier (numeric)
\item \code{event_time}: event time (numeric) for events strictly less than \code{follow_up}
}
}
\description{
Simulate recurrent event times in long format for a set of individuals.
For each individual the waiting times between events are drawn from
exponential distributions with state-specific rates and then cumulated to
event times. Event times that exceed the administrative follow-up are
censored (removed).
}
\details{
Algorithm summary:
\enumerate{
\item Construct a vector of per-event rates of length \code{max_events}:
\eqn{rate_j = lambda + b * (j - 1)}.
\item If \code{max_events} is not supplied, choose the smallest value in
3:20 for which the probability of experiencing all \code{max_events}
within \code{follow_up} is < 1e-4. When rates are constant the gamma
distribution (pgamma) is used; otherwise \code{sdprisk::phypoexp} is used.
\item Simulate waiting times with \code{rexp(n * max_events, rate = rates)}.
Rates are recycled to length \code{n * max_events} so that each subject
receives the block of per-event rates in order.
\item For each subject compute the cumulative sums of waiting times to obtain
event times, and retain only those event times < \code{follow_up}.
}

Notes and caveats:
\itemize{
\item Event times exactly equal to \code{follow_up} are excluded (strict <).
}
}
\examples{
# small example
recurr_event(id = 1:3, param = 0.1, b = 0, follow_up = 10, max_events = 5)

# let function choose max_events automatically
recurr_event(id = 1:10, param = 0.05, b = 0.01, follow_up = 30)

}
\seealso{
\code{\link[sdprisk]{phypoexp}} for hypoexponential CDF used internally
}
\keyword{recurr_event}
