% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vglm_helpers.R
\name{vglm_split_rcs}
\alias{vglm_split_rcs}
\title{Fit a VGAM model with splines split for column-level constraints}
\usage{
vglm_split_rcs(
  formula,
  family,
  data,
  constraints = NULL,
  control = VGAM::vglm.control(),
  model = FALSE,
  x.arg = TRUE,
  y.arg = TRUE,
  qr.arg = TRUE,
  ...
)
}
\arguments{
\item{formula}{Model formula passed to \code{VGAM::vglm()}.}

\item{family}{A VGAM family object, e.g. \code{VGAM::cumulative()}.}

\item{data}{Model data.}

\item{constraints}{Optional VGAM constraints list. Names should match the
column names in a full-PO fit returned by this helper. Factor terms may
still need to be supplied at the formula-term level, as in VGAM itself.}

\item{control}{Control list from \code{VGAM::vglm.control()}.}

\item{model, x.arg, y.arg, qr.arg}{Passed through to \code{VGAM::vglm()} for the
returned object.}

\item{...}{Currently ignored.}
}
\value{
A fitted \code{vglm} object.
}
\description{
\code{VGAM::vglm()} groups multi-column inline terms such as \code{rms::rcs(time, 4)}
under one model term when processing constraints. This helper keeps the
user-facing formula unchanged, but splits the internal assignment metadata
for inline restricted cubic spline columns before calling \code{VGAM::vglm.fit()}.
That makes it possible to apply different constraint matrices to individual
spline basis columns while still fitting a model written with inline
\code{rcs()}.
}
