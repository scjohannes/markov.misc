% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_trajectories.R
\name{sim_actt2_brownian}
\alias{sim_actt2_brownian}
\title{Simulate ACTT-2-Inspired Ordinal Trajectories}
\usage{
sim_actt2_brownian(
  n_patients = 1000,
  treatment_prob = 0.5,
  mu_treatment_effect = 0,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_patients}{Integer. Number of patients to simulate (default: 1000).}

\item{treatment_prob}{Numeric. Probability of assignment to treatment group
(default: 0.5).}

\item{mu_treatment_effect}{Numeric. Additional constant drift for treatment
group (default: 0, meaning no arm difference).}

\item{seed}{Integer. Random seed for reproducibility (default: NULL).}

\item{...}{Additional arguments passed to
\code{\link[=sim_trajectories_brownian_gap]{sim_trajectories_brownian_gap()}}. These can be used to override any of the
ACTT-2-inspired defaults such as thresholds, drift terms, or refresh rate.}
}
\value{
A tibble with the same columns returned by
\code{\link[=sim_trajectories_brownian_gap]{sim_trajectories_brownian_gap()}}.
}
\description{
Generates synthetic ACTT-2-like ordinal outcome data using the Brownian gap
simulator with defaults tuned to roughly match the control-arm state
occupancy, cumulative death, and low rehospitalization patterns described in
the ACTT-2 trial. The defaults are null-aligned, so there is no built-in
treatment difference unless \code{mu_treatment_effect} is changed.
}
\details{
This is intended as a convenient starting point for simulation studies, not
as an exact recreation of the original trial.
}
\examples{
\dontrun{
actt2_null <- sim_actt2_brownian(n_patients = 1000, seed = 123)

actt2_alt <- sim_actt2_brownian(
  n_patients = 1000,
  mu_treatment_effect = -0.03,
  seed = 123
)
}

}
