% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-delta-accessors.R
\name{markov.misc.delta_max_bytes}
\alias{markov.misc.delta_max_bytes}
\title{Analytical Delta-Method Memory Limit}
\description{
\code{markov.misc.delta_max_bytes} limits the size of guarded allocations used by
analytical delta-method SOP inference. The default is \code{256 * 1024^2} bytes
(256 MiB, or 268,435,456 bytes).
}
\details{
The limit is checked for the SOP Jacobian and recursion workspace, retained
low-rank analytical state, standard-error workspaces, and covariance or
Jacobian blocks requested through \code{\link[stats:vcov]{stats::vcov()}} or \code{\link[=get_jacobian]{get_jacobian()}}. Sizes
are measured in bytes. The checks estimate the principal numeric allocations
and are not a guarantee of total process memory use.

Increase the option only after confirming that the reported allocation and
additional R overhead fit comfortably in available memory. Restore the prior
option after a temporary override.
}
\examples{
old_options <- options(
  markov.misc.delta_max_bytes = 512 * 1024^2 # 512 MiB
)
options(old_options)
}
\seealso{
\code{\link[=inferences]{inferences()}}, \code{\link[=get_jacobian]{get_jacobian()}}, \code{\link[stats:vcov]{stats::vcov()}}
}
